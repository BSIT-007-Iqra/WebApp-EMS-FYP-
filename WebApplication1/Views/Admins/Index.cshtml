@model IEnumerable<WebApplication1.Models.Admin>

        <!-- Bread crumb and right sidebar toggle -->
        <!-- ============================================================== -->
        <div class="row page-titles">
            <div class="col-md-5 align-self-center">
                <h3 class="text-themecolor">Profile</h3>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="javascript:void(0)">Home</a></li>

                    <li class="breadcrumb-item active">Index</li>
                </ol>
            </div>
            <div class="col-md-7 align-self-center">
                <a href="@Url.Action("Create", "Admins")" class="btn waves-effect waves-light btn btn-info pull-right hidden-sm-down"> Create New</a>
            </div>

        </div>
        <!-- ============================================================== -->
        <!-- End Bread crumb and right sidebar toggle -->
        <!-- ============================================================== -->
        <!-- ============================================================== -->
        <!-- Start Page Content -->
        <!-- ============================================================== -->
        <!-- Row -->
        <div class="row">
            <table class="table table-responsive">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Role.Role_Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Admin_Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Admin_Email)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Admin_Password)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Admin_Contact)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Admin_Picture)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Status)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Role.Role_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Admin_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Admin_Email)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Admin_Password)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Admin_Contact)
                        </td>
                        <td>
                            <img src="@Url.Content(item.Admin_Picture)" height="100" width="100" />
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Status)
                        </td>
                        <td>
                            <a href="@Url.Action("Edit", "Admins", new { id=item.Admin_ID })">
                                <i class="fa fa-edit btn btn-success  "></i>
                            </a><br />

                            <a href="@Url.Action("Details", "Admins", new { id=item.Admin_ID})">
                                <i class="fa fa-book btn btn-warning"></i>
                            </a><br />
                            <a href="@Url.Action("Delete", "Admins", new { id=item.Admin_ID })">
                                <i class="fa fa-trash btn btn-danger"></i>
                            </a>
                        </td>
                    </tr>
                }

            </table>
        </div>
        <!-- Row -->
       