@model IEnumerable<WebApplication1.Models.Sub_ServiceCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layoutadmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Service_Category.ServiceCategory_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sub_category_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Service_Category.ServiceCategory_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sub_category_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubCategory_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubCategory_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubCategory_ID })
        </td>
    </tr>
}

</table>
