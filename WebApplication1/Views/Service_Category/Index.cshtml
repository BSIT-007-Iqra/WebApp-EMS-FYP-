@model IEnumerable<WebApplication1.Models.Service_Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layoutadmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ServiceCategory_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceCategory_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServiceCategory_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServiceCategory_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServiceCategory_ID })
        </td>
    </tr>
}

</table>
